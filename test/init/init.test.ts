import { describe, it } from 'bun:test';

describe('asena-cli init test', async () => {
  it.skip('should revert error if asena config already exists', () => {});

  it.skip('should revert error if asena config already exists', async () => {});
});
